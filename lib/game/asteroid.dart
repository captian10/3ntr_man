class Asteroid {
  Asteroid({
    required this.x,
    required this.y,
    required this.r,
    required this.speed,
    required this.drift,
  });

  double x, y, r, speed, drift;
  double rPx = 14;
}
